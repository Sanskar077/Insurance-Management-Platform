# Prisma

This folder will hold `schema.prisma` and migration files.

Prisma packages are installed, but the schema is intentionally not created yet —
it will be added in the Day 2 session (Database Design & Authentication).

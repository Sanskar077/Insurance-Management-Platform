node_modules
dist
build
.env
.env.local
*.log
coverage
.DS_Store

# Prisma generated client (regenerated via `prisma generate`)
generated

# Uploaded file content — folder structure is preserved via .gitkeep files,
# but actual uploaded documents are never committed.
uploads/customers/*
uploads/policies/*
uploads/claims/*
!uploads/customers/.gitkeep
!uploads/policies/.gitkeep
!uploads/claims/.gitkeep
